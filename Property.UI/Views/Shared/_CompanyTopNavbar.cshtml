<div class="row border-bottom">
    <nav class="navbar navbar-static-top white-bg" role="navigation" style="margin-bottom: 0">
        <div class="navbar-header">
            <a class="navbar-minimalize minimalize-styl-2 btn btn-primary " href="#"><i class="fa fa-bars"></i> </a>
        </div>
        <ul class="nav navbar-top-links navbar-right">
            @*<li class="dropdown">
                    <a class="dropdown-toggle count-info" data-toggle="dropdown" href="#">
                        <i class="fa fa-bell"></i>  <span class="label label-warning">@Html.GetNoDisposeQuestionNum()</span>
                    </a>
                    @if (Html.IsHasButton("/Question/QuestionList") && Html.GetNoDisposeQuestionNum() > 0)
                    {
                        <ul class="dropdown-menu dropdown-alerts">
                            @foreach (var que in Html.GetNewestNoDisposeQuestion())
                            {
                                <li>
                                    <a href="@Url.Action("QuestionList", "Question")">
                                        <div>
                                            <i class="fa fa-bell fa-fw"></i> @que.Title
                                            <span class="pull-right text-muted small">@que.TimeAgoStr</span>
                                        </div>
                                    </a>
                                </li>
                                <li class="divider"></li>
                            }
                            <li>
                                <div class="text-center link-block">
                                    <a href="@Url.Action("QuestionList", "Question")">
                                        <strong>查看更多</strong>
                                        <i class="fa fa-angle-right"></i>
                                    </a>
                                </div>
                            </li>
                        </ul>
                    }
                </li>*@

            <li>
                <a href="@Url.Action("CompanyPlatformLogOff", "Account")">
                    <i class="fa fa-sign-out"></i> 退出
                </a>
            </li>
        </ul>
    </nav>
</div>